import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import Concentration from './assets/Concentration';

export default class App extends React.Component {
  render() {
    return (
      <Concentration />
    );
  }
}
